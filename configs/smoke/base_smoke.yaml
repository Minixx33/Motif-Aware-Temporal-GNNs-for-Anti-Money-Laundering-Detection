experiment:
  seed: 999
  name: smoke_slt_low_1epoch
paths:
  root: ''
  graphs_dir: graphs
  tgat_graphs_dir: graphs_tgat
  dyrep_graphs_dir: graphs_dyrep
  splits_dir: splits
  tgat_splits_dir: splits_tgat
  dyrep_splits_dir: splits_dyrep
  results_dir: results
  logs_dir: logs
evaluation:
  threshold: 0.5
  auto_threshold: true
  compute_top_k: true
  top_k_values:
  - 100
  - 500
  - 1000
logging:
  print_interval: 1
  save_history: true
  save_best_only: true
sync:
  enabled: false
  rclone_remote: gdrive
  drive_root: AML_project_results
  sync_logs: true
  sync_every_n_epochs: 10
  extra_args: []
  rclone_path: C:\Users\g00084287\AppData\Local\miniconda3\envs\aml_project\bin\rclone.exe
